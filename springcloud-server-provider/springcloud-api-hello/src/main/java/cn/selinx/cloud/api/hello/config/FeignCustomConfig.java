package cn.selinx.cloud.api.hello.config;

import org.springframework.cloud.openfeign.FeignClient;

/**
 * {@link FeignClient }自定义配置类，用于个别服务自定义配置
 *
 * <p>
 * 可以不需要@Configuration注解，如果采用避免被Spring自动扫描到
 *
 * @author JiePeng Chen
 * @since 2019/8/13 17:18
 */
public class FeignCustomConfig {
}
